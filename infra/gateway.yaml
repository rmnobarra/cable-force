apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: cable-force-gateway
  namespace: default
spec:
  gatewayClassName: envoy-gateway-classic
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: Same
